COMPLETE SPRITE SET — drop the whole thing into your wiki's sprites/ folder.

WHAT'S HERE:
- 771 sprite PNGs named by dex number (NNN.png, NNN-f2.png, NNN-back.png,
  NNN-shiny.png, NNN-shiny-f2.png, NNN-shiny-back.png, NNN-icon.png)
- manifest.json listing all 114 dex numbers that have a front sprite

HOW TO USE:
1. Copy ALL of these files into  Void-Wiki-main/wiki/sprites/  (overwrite)
   — actually the sprites/ folder, wherever index.html points (sprites/).
2. Make sure manifest.json lands in sprites/ too (it replaces the old 2-entry one).
3. Hard-refresh (Ctrl+Shift+R).

Every mon currently in the pack now has art, and the manifest lists them all,
so as you add their data entries the sprites are already there. No more sprite work.

NOT INCLUDED (need a dex number assigned before they can be added):
- PALAZARD2  (ambiguous — base Palazard #096 and Mega #096-M are already mapped;
  this looks like a 3rd variant. Tell me its dex/form and I'll add it.)
- Anomaly sprites (intentionally excluded — kept as "undiscovered")
- Form variants (_M Mega, _1, KAMOLEON_High/Low/Medium) — these are alternate
  forms that need form-entry data, not base dex slots.
- 4 extra Sedis (SEDIGIANT/SEDIMITE2/SEDINEY/SEDISIRE) — excluded per request.
